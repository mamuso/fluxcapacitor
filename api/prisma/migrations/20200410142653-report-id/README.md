# Migration `20200410142653-report-id`

This migration has been generated by Manuel Muñoz Solera at 4/10/2020, 2:26:53 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Report" ADD COLUMN "pageId" text   ;

ALTER TABLE "public"."Capture" ADD COLUMN "pageId" text   ,
ADD COLUMN "reportId" text   ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200409225613-scaffold..20200410142653-report-id
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("POSTGRES_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -12,9 +12,10 @@
   slug      String    @unique
   createdAt DateTime  @default(now())
   captures  Capture[]
   url       String?
-  current   Boolean  @default(false)
+  current   Boolean   @default(false)
+  pageId    String?
 }
 model Page {
   id        String    @default(cuid()) @id
@@ -24,21 +25,23 @@
   createdAt DateTime  @default(now())
 }
 model Capture {
-  id          String    @default(cuid()) @id
-  slug        String
-  url         String?
-  urlmin      String?
-  urldiff     String?
-  diff        Boolean   @default(false)
-  device      String
-  createdAt   DateTime  @default(now())
+  id        String   @default(cuid()) @id
+  slug      String
+  url       String?
+  urlmin    String?
+  urldiff   String?
+  diff      Boolean  @default(false)
+  device    String
+  createdAt DateTime @default(now())
+  reportId  String?
+  pageId    String?
 }
 model Device {
-  id        String    @default(cuid()) @id
-  slug      String    @unique
+  id        String   @default(cuid()) @id
+  slug      String   @unique
   name      String
   specs     String
-  createdAt DateTime  @default(now())
+  createdAt DateTime @default(now())
 }
```


